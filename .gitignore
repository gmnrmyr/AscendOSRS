# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Dados reais dos bancos (local-only, nao versionar)
prototype/fixtures/
.env.local

# Save do dashboard: versionado de propósito (repo privado, projeto pessoal).
# data/dashboard.json viaja no push pra sincronizar Mac <-> desk.
# (o .runlog/ de logs continua ignorado)
.runlog/

# backups diários rotativos do server (recuperação local; o git já versiona o dashboard.json)
data/backups/
